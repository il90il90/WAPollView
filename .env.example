# PostgreSQL
POSTGRES_USER=polluser
POSTGRES_PASSWORD=pollpass_secure_2024
POSTGRES_DB=whatsapp_polls
DATABASE_URL=postgresql://polluser:pollpass_secure_2024@postgres:5432/whatsapp_polls

# Frontend
FRONTEND_PORT=9090

# Admin password is configured via the UI on first launch (stored in DB)
